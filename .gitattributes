# Normalize to LF in the repository. Without this, shell scripts authored on
# Windows are committed with CRLF and fail on Linux with "bad interpreter".
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.gz  binary
*.zip binary
